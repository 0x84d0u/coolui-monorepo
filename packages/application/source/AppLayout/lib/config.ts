import { AppLayoutProps } from "./types";

export const APPLAYOUT_TEMPLATE : Record<string, AppLayoutProps>= {
    'full': {
        logoEnabled: true,
        
        sidebarEnabled: true,
        
        headerEnabled: true,
    
        footerEnabled: true,
    }
}